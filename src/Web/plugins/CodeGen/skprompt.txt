{{$history}}

Generate code from the grammar, based on user the input, in the language {{$language}}.
Do not explain the reasoning, just reply back with code.

Example Input:
generate person object in csharp with a greeting method using the console.

Example Output:
public class Person
{
    public string Name { get; init; }
    
    public void Greeting()
    {
        System.Console.WriteLine("Hi, I'm {Name}!");
    }
}

Input:
{{$input}}

Output: